<h1>All Posts</h1>
<% if user_signed_in? %>
  <%= link_to "New Post", new_post_path %>
<% end %>
<% @posts.each do |post| %>
  <div class="post">
    <h2><%= link_to post.title, post_path(post) %></h2>
    <p><%= post.body %></p>
    <p>Posted by: <%= post.user.email %></p>
  </div>
<% end %>