<h1>All Posts</h1>
<%= link_to "New Post", new_post_path %>
<% @posts.each do |post| %>
  <div class="post">
    <h2><%= link_to post.title, post_path(post) %></h2>
    <p><%= post.body %></p>
    <p>Posted by: <%= post.user.email %></p>
  </div>
<% end %>